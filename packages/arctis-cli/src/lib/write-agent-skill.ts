/**
 * Write the composed skill package to agent-specific directories.
 * Placeholder — will write to .claude/skills/arctis, etc.
 */
export function writeAgentSkill(_agent: string, _content: string, _cwd: string) {
  console.log("writeAgentSkill: coming soon");
}
